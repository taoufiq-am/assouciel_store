stages:
  - test
  - deploy

variables:
  APP_ENV: "production"
  APP_KEY: "base64:+c2Y1DdGUZum0HZ8j9E81AxchR2th43inHvohwxofng="
  DB_CONNECTION: "mysql"
  DB_HOST: "localhost"
  DB_DATABASE: "tp1_elequent"
  DB_USERNAME: "root"
  DB_PASSWORD: ""

before_script:
  - apt-get update -qy
  - apt-get install -y unzip php php8.2-xml php8.2-curl php8.2-dom  # Install required packages, including PHP extensions
  - cp .env.example .env
  - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
  - composer install --no-ansi --no-interaction --no-progress --optimize-autoloader

script:
  - php artisan key:generate
  - php artisan migrate --force
  - php artisan test

test:
  stage: test
  script:
    - vendor/bin/phpunit

deploy:
  stage: deploy
  script:
    - echo "deploy"
